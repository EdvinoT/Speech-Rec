
    ('en', 'English'),
    ('es', 'Spanish'),
    ('fr', 'French'),
    ('de', 'German'),
    ('it', 'Italian'),
    ('pt', 'Portuguese'),
    ('ja', 'Japanese'),
    ('ko', 'Korean'),
    ('zh', 'Chinese')
]

# Display language options
for i, (code, name) in enumerate(languages, start=1):
    print(f"{i}. {name} ({code})")

# Ask the user to choose a language
try:
    choice = int(input("Enter the number of your chosen language: ")) - 1
    if 0 <= choice < len(languages):
        language = languages[choice][0]
        print(f"Selected language: {languages[choice][1]}")
    else:
        print("Invalid choice. Defaulting to English.")
        language = 'en'
except ValueError:
    print("Invalid input. Defaulting to English.")

   
   
      print("Available languages:")
    for code, name in languages:
        print(f"{code}: {name}")

    lang_input = input("Enter the language code (e.g., 'en' for English): ").strip().lower()
    if lang_input in dict(languages):
        language = lang_input
        print(f"Selected language: {dict(languages)[language]}")
    else:
        print("Invalid language selection. Defaulting to English.")
        language = 'en'